# Nix build output
/result
/result-*
.envrc
.direnv/

# >>> buildflow-managed (.gitignore) >>>
# Auto-managed by buildflow. Edit outside these markers; this block is regenerated on `buildflow --fix`.

## Secrets and environment
.env
.env.local
.env.*.local
*.pem
*.key

## Databases
*.db
*.db-shm
*.db-wal

## Operating systems
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db

## Editors and ides
.idea/
.vscode/
*.swp
*.swo
*~

## Logs and temporaries
*.log
*.tmp
*.temp

## Buildflow artifacts
audit-log.*
workflow-audit-log.*
.buildflow-workflow-baseline.json

## Reports and coverage
/coverage
reports/

## Go
*.exe
*.dll
*.so
*.dylib
*.test
*.out
vendor/
go.work
go.work.sum
/go-linter-sdk
# <<< buildflow-managed (.gitignore) <<<

# Keep the reports/ directory present on fresh clones. BuildFlow's
# test-coverage step writes reports/coverage.out and fails if the directory is
# missing. The managed block above ignores reports/ wholesale, so re-include
# the directory itself, ignore its contents, then keep only the marker file.
!reports/
reports/*
!reports/.gitkeep
